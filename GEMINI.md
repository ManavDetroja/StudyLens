# Development Rules (GEMINI)

- Keep JavaScript modular; pp.js only boots the app.
- Use HTML for static structure; avoid generating UI in JS.
- TypeScript only for essential data models.
- No external frameworks or backend services.
- Persist data in IndexedDB; use localStorage for small settings.
